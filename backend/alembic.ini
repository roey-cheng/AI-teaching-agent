[alembic]
# 路径以本配置文件所在目录为基准，也支持从项目根目录使用 -c 调用。
script_location = %(here)s/alembic
prepend_sys_path = %(here)s
path_separator = os
output_encoding = utf-8

# 不在此处保存数据库 URL 或密码。在线迁移由 env.py 读取 backend/.env。
